import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

export const metadata: Metadata = {
  title: { default: "Bloom Studio | Custom Bouquet Builder", template: "%s | Bloom Studio" },
  description: "Build custom bouquets in real time and schedule flower delivery.",
  openGraph: { title: "Bloom Studio", description: "Custom flower shop e-commerce with live bouquet builder.", type: "website" }
};

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return <html lang="en"><body className={`${geistSans.variable} ${geistMono.variable}`}>{children}</body></html>;
}
